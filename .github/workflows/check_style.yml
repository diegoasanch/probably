name: Check code style with prettier

on: [pull_request]

jobs:
  prettier:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/setup-node@v2
        with:
          node-version: 14

      - name: Install prettier
        run: yarn add --exact prettier

      - uses: actions/checkout@v2

      - name: Check style
        run: yarn prettier -c .
